/* global expect */
describe('calculator', function() { //describe specifies a "spec" : logical grouping of tests

    it('1 + 1 should equal 2', function() {
        expect(1 + 1).toBe(2);
    }); // it specifies a single test within a spec


});
